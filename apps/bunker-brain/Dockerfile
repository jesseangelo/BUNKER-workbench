# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build bunker-brain`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t bunker-brain`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system bunker-brain && \
          adduser --system -G bunker-brain bunker-brain

COPY dist/apps/bunker-brain bunker-brain
RUN chown -R bunker-brain:bunker-brain .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix bunker-brain --omit=dev -f install

CMD [ "node", "bunker-brain" ]
